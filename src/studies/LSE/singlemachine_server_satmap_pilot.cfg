LSE_GameServer
client_hosts = ['localhost:3663','localhost:3664']
available_attention_set = ['satellite map icons'] 		# we're piloting only the satellite map for now...
secure_perimeter_duration = [15000,20000]			# the perimeter mission goes on forever
mission_override = 'coop-movetogether'